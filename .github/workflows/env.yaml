name: env

on:
  workflow_dispatch:

env:
  A: workflow

jobs:
  job:
    runs-on: ubuntu-latest
    env:
      B: job
    steps:
      - env:
          C: step
        run: |
          echo "$A" "$B" "$C"
          env

      - env:
          exists: ${{ env.B != '' }}
        if: env.exists == 'true'
        run: |
          echo "$A" "$B" "$C"
          env

      - env:
          exists: ${{ env.C != '' }}
        if: env.exists == 'true'
        run: |
          echo "$A" "$B" "$C"
          env
